# ───────── Node (backend) ─────────
node_modules/
npm-debug.log*
*.log

# Secrets
.env
.env.*
!.env.example

# ───────── Android ─────────
android/.gradle/
android/build/
android/app/build/
android/captures/
android/local.properties
*.iml
.idea/
*.keystore
*.jks
# ...except the committed app-signing keystore, which must be shared across all
# builds so in-app updates install over previous versions (see app/build.gradle.kts).
!android/app/alexstream.jks

# APK / build outputs
*.apk
*.aab

# ───────── Python / Modal ─────────
__pycache__/
*.pyc
.venv/
venv/

# ───────── OS / editor ─────────
.DS_Store
Thumbs.db
KEYSTORE_SECRETS.local.txt
